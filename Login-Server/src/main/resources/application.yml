spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8
    username: root
    password: aa123123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  http:
    converters:
      preferred-json-mapper: gson
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    timeout: 3000ms
    jedis:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms
  application:
    name: login-server-01
server:
  servlet:
    context-path: /rojee
  port: 8080
social:
  wechat:
    providerId: weixin
    appid: 45fdfsfssdf756
    secret: 9453453fdgfdgf435456
server-info:
  id: 1
  type: login
  ip: 127.0.0.1
  port: 8080
eureka:
  client:
    service-url:
      defaultZone: http://user:123456@localhost:1110/master-server/
    enabled: true
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: localhost
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 15
    instance-id: ${spring.application.name}